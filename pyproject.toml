[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "container-crate-modeling"
version = "0.1.0"
description = "Single ULD profile packing prototype"
requires-python = ">=3.12"
dependencies = []

[tool.setuptools]
packages = ["cargo_loading"]
